{
  "name": "agiledev-suite",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -n fe,be -c cyan,green \"npm run dev:fe\" \"npm run dev:be\"",
    "dev:fe": "npm run dev --prefix frontend",
    "dev:be": "npm run dev --prefix backend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
